version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/database/queries"
    schema: "internal/database/migrations"
    gen:
      go:
        # attaches json tags to structs
        emit_json_tags: true
        package: "database"
        out: "internal/database"
        sql_package: "pgx/v5"
        overrides:
          # replaces PG UUID with google UUID
          - db_type: "uuid"
            nullable: false
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
          # replaces PG timestamps with time.Time
          - db_type: "timestamptz"
            go_type:
                import: "time"
                type: "Time"
